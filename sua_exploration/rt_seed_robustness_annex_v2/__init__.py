"""CPU-only RT seed robustness annex v2.

Version 2 supersedes the v1 draft's score-dependent hash ranking.  It keeps
v1 immutable and uses only a fixed fold-ID rule; it is still a draft and does
not launch a GPU run.
"""

__all__: list[str] = []
