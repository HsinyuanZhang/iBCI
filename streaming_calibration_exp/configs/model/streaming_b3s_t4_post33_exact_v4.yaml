_target_: src.models.streaming_post33_exact_t4_v4_module.M2Post33ExactPairedT4LitModuleV4

task: m2
variant: B3S
paired_spint_completion_receipt: ???
# These are not optional convenience paths: the model resolver derives the
# owning T4 cell from this immutable ownership record and rejects a receipt
# from any other Phase-C root before it opens teacher weights.
phase_c_t4_owner_path: ???
phase_c_t4_owner_token: ???
loso_fold: ${data.loso_fold}
seed: ${seed}
teacher_receipt_path: null
require_clean_teacher_receipt: false
window_size: ${data.window_size}
trial_length: ${data.max_trial_length}
pad_value: ${data.pad_value}
freeze_decoder: true
encoder_warmstart_path: null
freeze_encoder_base: false
tune_encoder_fusion: false
fusion_mean_lr_scale: 1.0
loss_mode: task_plus_y_plus_E
lambda_y: 1.0
lambda_E: 0.1
decode_last_timestep_only: true
predict_scaled_behavior: true
behavior_scaling_factor: 5.0
id_hidden_dim: 128
hidden_dim: 64
num_emas: 4
num_filters: 4
kernel_size: 5
learnable_ema_alpha: false
side_dim: 4
electrode_embed_dim: 0
num_electrodes: 0
optimizer:
  _target_: torch.optim.Adam
  _partial_: true
  lr: 1.0e-4
  weight_decay: 0.0
scheduler: null
compile: false
neuron_dropout_mode: none
neuron_dropout_p_low: 0.0
neuron_dropout_p_high: 0.3
neuron_dropout_block_size: 4
neuron_dropout_warmup_epochs: 10
support_prediction_consistency_weight: 0.0
