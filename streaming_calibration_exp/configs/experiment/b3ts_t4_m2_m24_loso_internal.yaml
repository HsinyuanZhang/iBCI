# @package _global_

defaults:
  - override /model: streaming_b3ts_t4
  - override /data: falcon_m2

run_id: native_mua_m24_b3ts_t4_m2_loso_internal
no_early_stopping: true
tags: ["native-mua", "b3ts", "t4", "m2", "loso", "internal-only", "m24-b3ts"]

data:
  validation_protocol: loso
  loso_fold: 1
  calibration_n_trials: 24
  random_calibration: false
  include_heldout_in_fit: false
  include_heldout_in_test: false
  query_start_trial: 0
  side_feature_group: t4

model:
  freeze_decoder: true

trainer:
  max_epochs: 12

callbacks:
  early_stopping: null
