# @package _global_

defaults:
  - override /model: streaming_b3

run_id: b3_d64_dropout_standard
tags: ["b3", "d64", "dropout", "uniform", "standard"]
model:
  hidden_dim: 64
  neuron_dropout_mode: uniform
  neuron_dropout_p_low: 0.0
  neuron_dropout_p_high: 0.30
