# @package _global_

defaults:
  - override /model: streaming_b3_m1
  - override /data: falcon_m1

run_id: native_mua_f0_m1_loso_internal
no_early_stopping: true
tags: ["native-mua", "f0", "m1", "loso", "internal-only"]

data:
  validation_protocol: loso
  loso_fold: 0
  include_heldout_in_fit: false
  include_heldout_in_test: false
  random_calibration: false

trainer:
  max_epochs: 12

callbacks:
  early_stopping: null
