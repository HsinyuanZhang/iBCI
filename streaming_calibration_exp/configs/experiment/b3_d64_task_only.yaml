# @package _global_

defaults:
  - override /model: streaming_b3

run_id: b3_d64_task_only
comparison_role: loss_ablation
tags: ["b3", "d64", "ablation", "task_only", "loso", "r1"]
data:
  validation_protocol: loso
  include_heldout_in_fit: false
  include_heldout_in_test: false
model:
  hidden_dim: 64
  freeze_decoder: true
  loss_mode: task_only
  lambda_y: 0.0
  lambda_E: 0.0
