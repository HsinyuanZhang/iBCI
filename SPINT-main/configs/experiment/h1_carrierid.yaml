# @package _global_
defaults:
  - override /data: falcon_h1_m4_eb_normalized_v2
  - override /model: falcon_h1_carrierid
  - override /callbacks: h1_carrierid_terminal

protocol_id: h1_carrierid_h32_fold0_source_only_v1
task_name: h1_carrierid_h32_full
tags: [h1, carrierid, h32, m4, normalized_carrier, fold0, full]
seed: 42
train: true
test: false
ckpt_path: null
clean_teacher_mode: false
optimized_metric: null
logger: false

pilot:
  fold_date: "19250101"
  arm: full
  zero_carrier: false
  shared_cache_dir: ${paths.work_dir}/pilot_artifacts/h1_carrierid/shared_source_cache
  calibration_n_trials: 4
  batch_size: 32
  fixed_terminal_epochs: 50
  no_checkpoint_selection: true

trainer:
  accelerator: gpu
  devices: 1
  max_epochs: 50
  min_epochs: 50
  precision: 32-true
  deterministic: false
  enable_checkpointing: true
  limit_val_batches: 0
  num_sanity_val_steps: 0
  check_val_every_n_epoch: 1
