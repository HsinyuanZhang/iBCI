# @package _global_
defaults:
  - override /data: falcon_h1_sparse_event_endpoint_dated
  - override /model: falcon_h1_sparse_event_endpoint_dated
  - override /callbacks: h1_carrierid_terminal

protocol_id: h1_sparse_event_endpoint_q4_ridge3_lodo_m4_date2_v1
task_name: h1_hse5_lodo_zero5_19250108
tags: [h1, hse5, sparse_event, endpoint, q4, ridge3, lodo, m4, zero5, 19250108]
seed: 42
train: true
test: false
ckpt_path: null
clean_teacher_mode: false
optimized_metric: null
logger: false
pilot:
  fold_date: "19250108"
  arm: zero
  zero_carrier: true
  shared_cache_dir: ${paths.work_dir}/pilot_artifacts/h1_hse5_lodo_19250108/shared_source_cache
  source_snapshot_receipt: ${paths.work_dir}/pilot_artifacts/h1_hse5_lodo_19250108/H1_HSE5_LODO_19250108_SOURCE_v1.json
  calibration_n_trials: 4
  batch_size: 32
  fixed_terminal_epochs: 50
  no_checkpoint_selection: true
trainer:
  accelerator: gpu
  devices: 1
  max_epochs: 50
  min_epochs: 50
  precision: 32-true
  deterministic: false
  enable_checkpointing: true
  limit_val_batches: 0
  num_sanity_val_steps: 0
  check_val_every_n_epoch: 1
