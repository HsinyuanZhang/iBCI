_target_: src.models.h1_carrierid_date_lodo_est4_module.H1CarrierIdDateLodoEst4LitModule
task: h1
arm: ${phase_est4.arm}
outer_date: ${phase_est4.outer_date}
fixed_seed: 42
est4_preflight_path: ${phase_est4.est4_preflight_path}
five_date_aggregate_path: ${phase_est4.five_date_aggregate_path}
frozen_plan_path: ${phase_est4.frozen_plan_path}
decode_last_timestep_only: true
predict_scaled_behavior: true
behavior_scaling_factor: 20.0
optimizer:
  _target_: torch.optim.Adam
  _partial_: true
  lr: 5.0e-5
  weight_decay: 0.0
scheduler: null
compile: false
clean_teacher: true
scheduler_monitor: val_heldin/r2_mean
net:
  _target_: src.models.components.h1_carrierid_est4_spint.H1CarrierIdEst4Spint
  estimator_mode: learned
  frozen_plan_path: ${phase_est4.frozen_plan_path}
  # This placeholder is overwritten from the immutable source binding before
  # the first source forward; the config cannot set a target-derived scale.
  source_normalizer: 1.0
  zero_carrier: false
  row_shuffle_output: false
  carrier_hidden_dim: 32
  carrier_dim: 4
  carrier_trial_length: 1024
  model_dim: 1024
  num_covariates: 7
  window_size: 700
  num_heads: 64
  num_layers: 1
  num_id_layers: 3
  use_learnable_id: true
  learnable_id_type: mlp
  learnable_rep: true
  dropout_rate: 0.0
  dynamic_dropout: true
  dynamic_dropout_low: 0.0
  dynamic_dropout_high: 1.0
  tf_drop_rate: 0.1
  readin_layer_type: mlp
