_target_: src.data.h1_sparse_event_endpoint_dated.H1SparseEventDatedDataModule
task: h1
data_dir: ${paths.data_dir}/000954
cache_dir: ${pilot.shared_cache_dir}
fold_date: ${pilot.fold_date}
source_snapshot_receipt: ${pilot.source_snapshot_receipt}
batch_size: 32
window_size: 700
calibration_n_trials: 4
max_trial_length: 1024
num_workers: 0
pin_memory: false
seed: 42
fixed_epochs: 50
