# AJPF V3 score-only successor

V3 never retrains. It binds the sealed V2 outer failure, V2 successful nested
training graph and checkpoints, and V2 failed nested score prefix. A fresh
CPU-only score attempt first publishes its immutable V3 attempt, then performs
the existing materializer's exact 13-key POOLED sentinel comparison. Only a
13/13 exact authority receipt may invoke the unchanged V1 CPU scorer beneath a
fresh nested V3 score root. No scorer/gate science is changed.
