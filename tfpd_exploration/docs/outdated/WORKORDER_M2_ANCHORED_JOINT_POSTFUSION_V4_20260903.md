# AJPF V4 workorder

1. Issue only `issue_after_independent_review(reviewed_map, digest, repo,
   repo/ROOT_RELATIVE)`. It is one-shot and exact; the public CLI is inert.
2. In the fresh V4 root, publish an attempt before importing Torch or opening
   target/data/checkpoint material. The CPU environment is empty
   `CUDA_VISIBLE_DEVICES`, `CUDA_DEVICE_ORDER=PCI_BUS_ID`,
   `CUBLAS_WORKSPACE_CONFIG=:4096:8`, hash seed zero, no user site/no bytecode,
   OMP/MKL/OpenBLAS/NumExpr one, and repository-only `PYTHONPATH`.
3. Hold and admit the exact V3 failure graph and incident, V2 terminal
   training/checkpoint graph, and historical POOLED GPU score SHA-256
   `455485bd854a36392f17ec5ed029b1a4044a01a8dd7dfeae95c7763b8327f5f6`.
4. Perform exactly one CPU strict preparation/materialization. Publish a
   detailed all-13-key bridge. Require exact target/start/window witnesses
   13/13 and `max(abs(R2_CPU-R2_GPU)) <= 2e-7`; record, but do not require,
   prediction SHA equality.
5. Only after the bridge passes, invoke unchanged V1
   `score_whole_stack_78_cpu` and `build_preregistered_gates` on that same
   materialization and sealed GPU POOLED R² comparator. Publish input, 78-row
   score, gates, and terminal; otherwise seal the honest failure prefix.
   Revalidate root/parent identities, closure, environment, and all held
   authorities before either final leaf.

No training, CUDA/GPU access, V1--V3 mutation, POOLED mutation, bridge
relaxation, or gate/science change is authorized.
