# Corrected C2/C3 Posterior-Input Score V3

V3 is the final preflight-complete scoring successor.  Bind the exact V1
namespace failure and V2 missing-environment failure.  Before creating an
attempt, require empty CUDA visibility, exact canonical sub-C/sub-M data-root
environment values, non-symlink canonical directories, correct independent
TFPD `src` and streaming `models` namespace origins, the current strict
execution closure, and a fresh V3 result path.

Do not change any C2/C3 weights, posterior-input rule, target roster, support
selection, comparator, numerical decision threshold, or scoring formula.
Never retrain C2/C3 for either predecessor failure.  Review-only work-order
and test drift remains `ACCEPTED_NON_NUMERIC_DRIFT`; model/data/runtime or
numerical drift remains fail-closed.

