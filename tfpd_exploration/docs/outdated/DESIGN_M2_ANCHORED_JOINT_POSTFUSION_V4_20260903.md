# AJPF V4 final CPU score successor

## Scope and immutable predecessors

V4 is an additive, CPU-only score successor. It never retrains, does not alter
V1--V3 code or result roots, and does not change the 78-row scorer, two
preregistered gates, session roster, laws, or scientific setting. Before a new
root may be reserved, an independently reviewed opaque capability binds the
complete combined V4/V3/V2/V1 closure. The route holds, with `O_NOFOLLOW`, the
exact three-body/six-leaf V3 failure graph, the successful sealed V2 training
graph including three epoch-12 checkpoints, and the historical GPU POOLED
score. The V3 incident is
`RESULT_M2_ANCHORED_JOINT_POSTFUSION_V3_20260903.md`, SHA-256
`eb6d573df145dda10026b805d7cdd39b7974db4c08b3911fcaa5ac3a30fe020a`.

## Cross-device bridge

One strict CPU module preparation and one 13-session materialization are
allowed only after the immutable V4 attempt receipt.
`cross_device_bridge.json` is published before AJPF score rows or gates. For
every key it records observed CPU and held historical-GPU values for prediction
SHA-256, target SHA-256, query-start SHA-256, window count, and R². The route
fails closed unless all 13 target digests, all 13 query-start digests, and all
13 window counts match, and the largest absolute CPU/GPU R² difference is at
most `2e-7`. Prediction-hash equality is merely counted and reported honestly;
it is explicitly not an equality requirement.

The unchanged `score_whole_stack_78_cpu` receives the exact modules and
materialization from that bridge. The unchanged `build_preregistered_gates`
uses the sealed historical GPU POOLED R² as the practical comparator.

## Lifecycle

The outer graph is the immutable ordered prefix `attempt`,
`predecessor_authority`, `cross_device_bridge`, `input_authority`, `score`,
`gates`, then exactly one terminal or failure leaf. Every body and sidecar is
mode 0444/nlink 1. Root/parent identity, combined closure, frozen CPU
environment, and held predecessor witness are repeated immediately before the
terminal or failure leaf.
