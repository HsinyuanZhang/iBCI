# AJPF V2 successor workorder

1. Hold the exact immutable V1 failure graph through no-follow descriptors:
   `attempt.json`, `launch.json`, and `failure.json` plus their sidecars.
   Bind incident authority
   `RESULT_M2_ANCHORED_JOINT_POSTFUSION_V1_20260903.md` SHA-256
   `97396859268e2753a228b5d8c0c330ddadde8b9fbfac279246779953f776fdd5`.
2. Do not modify or retry V1.
3. Run the bounded source-only CPU strict-load state regression using the V2
   holder digest seam.  Require the frozen selected digest and no CUDA.
4. Reserve only `tfpd_exploration/results/m2_anchored_joint_postfusion_v2/{training,score}`.
   The strict-digest regression is an admission receipt.  Then execute the
   unchanged shared three-arm × twelve-epoch GPU0 loop and fresh CPU-only
   13-session/78-row scoring route, with the unchanged two gates.  V2 owns
   route schemas/root identity/predecessor binding; it must not copy science.
   The nested `training/` and `score/` graphs intentionally retain V1 receipt
   schemas: they are the validated unchanged scientific executor.  V2 owns
   the enclosing attempt/incident/admission/terminal graph and links both
   nested V1 terminals by body digest.
