"""Additive PMC-D matched-score V2 successor package.

The V2 route is a new result-root namespace.  It composes the reviewed V1
scientific contract and physical scorer, adds an immutable failed-V1 graph
gate, and fixes only the sealed Cell-D SWA producer-schema seam.
"""
