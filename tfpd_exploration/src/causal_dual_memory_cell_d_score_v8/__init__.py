"""Additive V8 physical-helper successor for the CDM-D matched scorer.

The package has no physical side effects at import time.  Its public CLI is a
static plan only; the reviewed physical route remains behind an opaque,
in-process root capability.
"""

