"""Inference-only activity-memory-law scan on the M2 LOCAL protocol (V1).

Frozen laws live in :mod:`.plan`; the driver in :mod:`.physical`; gate and
verdict evaluation in :mod:`.gates`; the recency memory laws in
:mod:`.memory`.  Nothing under any frozen result root is created or modified.
"""
